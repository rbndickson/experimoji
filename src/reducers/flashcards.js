import { initialFlashcardsState } from "../flashcardData";

function flashcards(state = initialFlashcardsState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default flashcards;
